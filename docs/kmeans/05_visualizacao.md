# 05 - Visualização e Relatório (K-Means)

Objetivo

- Visualizar os clusters gerados e fornecer recomendações operacionais.

Imagens incorporadas

![PCA 2D](imagens/pca_2d.png)

![Clusters (k=2)](imagens/kmeans_clusters.png)

Relatórios e arquivos

- `kmeans_clusters.csv` — labels por funcionário.
- `kmeans_X.csv` — features processadas usadas pelo modelo.
- `kmeans_model.joblib` — modelo salvo.

Próximos passos sugeridos

- Gerar um arquivo CSV com estatísticas por cluster (médias e contagens) — posso gerar automaticamente.
- Incluir variáveis categóricas e re-treinar para testar se os clusters melhoram.

Se quiser que eu gere o relatório por cluster agora, responda: perfil
